# Render Blueprint Configuration for EchoBreaker
# https://render.com/docs/blueprint-spec

services:
  - type: web
    name: echobreaker-mil
    runtime: static
    buildCommand: ""
    staticPublishPath: .
